import{describe,expect,it}from"vitest";import{newAchievementDraft,newCertificationDraft,toAchievementInput,toCertificationInput}from"./credentials";
describe("credential drafts",()=>{it("creates independent certification and achievement inputs",()=>{const certification=newCertificationDraft();certification.name="Cloud Professional";const achievement=newAchievementDraft();achievement.title="Engineering Award";expect(toCertificationInput(certification)).not.toHaveProperty("key");expect(toCertificationInput(certification).name).toBe("Cloud Professional");expect(toAchievementInput(achievement).title).toBe("Engineering Award");expect(certification.key).not.toBe(achievement.key)})});
